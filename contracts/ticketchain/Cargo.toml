[package]
name = "ticketchain"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "21.3.0"

[dev-dependencies]
soroban-sdk = { version = "21.3.0", features = ["testutils"] }

[workspace]

